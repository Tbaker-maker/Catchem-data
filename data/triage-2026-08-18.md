# Non-live SKU Triage — 2026-08-18 run (11 SKUs, pre-classified)

Evidence: filterReports from the 04:43 UTC clean run. Execute per class;
validate per CLAUDE.md before committing. Numbers marked (verify) need
current-market comps checked before setting — do not trust my estimates.

## Class A — Policy retirement (1)
- **sv3pt5-booster-bundle-jp** ("151 Japanese"): 150 fetched, 150 failSet —
  and English-only policy retires it regardless. ACTION: remove the SKU
  (or comment out with a note). Do not tune filters for it.

## Class B — Ceiling-too-low, hot special sets (3) — same bug family as vintage
Server-side price window sits BELOW the real market, so only junk gets fetched:
- **sv4pt5-etb** Paldean Fates ETB: prev $479.99 vs etb ceiling 400 → only 8
  cheap matches fetched. ACTION: per-SKU priceCeiling ~800 (verify comps).
- **sv4pt5-bb** Paldean Fates Booster Bundle: prev $250 vs bundle ceiling
  100 → 2 fetched. ACTION: per-SKU priceCeiling ~500 (verify).
- **swsh7-etb** Evolving Skies ETB: prev $15 = CONTAMINATED (packs) — the
  safety correctly refused to republish. Real market far above 400 ceiling.
  ACTION: per-SKU floor ~150 / ceiling ~900 (verify), add dataQualityNote
  on pre-fix history like its booster box got.

## Class C — Vintage pack-ocean (6): base1, base2 (Jungle), base3 (Fossil),
## neo1, hgss1-booster-box, bw1-booster-box
Mechanism: vintage bypass grants the GLOBAL $5 floor + sort=price ascending
→ the cheapest 150 matches are packs/singles; genuine boxes ($800–$8,000+)
never enter the window. 100% type/set fails are the symptom, not the cause.
ACTION: web-verify current sealed box comps per set, then set explicit
per-SKU priceFloor (+ ceiling where $10k global cap is too low for 1st Ed —
track Unlimited only unless Tyler says otherwise). Then revalidate each
with title samples. Do NOT loosen title filters to "fix" these.

## Class D — Thin-and-verify (1)
- **sm12-etb** Hidden Fates ETB: 22 fetched, 16 failType, kept 2. Likely a
  thin+junky query, prev $223 plausible. ACTION: sample the 16 type-fails'
  titles; if they're tins/collections, filter is CORRECT and the SKU is
  just thin — consider floor ~120 (verify) to enrich the fetch window.

## Anomaly to check (mechanism, not SKU)
- sv4pt5-etb triggered query_error at kept=5 — my original rule fired only
  at kept<3 with prev≥10 listings. Verify the current safety-threshold
  implementation; if it was tightened, document it in CLAUDE.md.
