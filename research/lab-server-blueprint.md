# Catchem Lab — 3-minute server blueprint (also the real-server template)
CHANNELS: #general (Eden lives here) · #raffles (embeds+entries) ·
#draws (provably-fair results + track record) · #rip-or-hold ·
#alerts (Morning Pulse webhook) · #audit-log (admin actions, private) ·
#rules (pin the rules template link)
ROLES: Admin (you) · Raffle Admin (appointees — maps to the bot's
role-gated list) · Member
SETTINGS: Community features ON (enables rules screening later) ·
Eden config: no-drop channels = raffles/draws/alerts/audit-log
