# v2-CI First-Run Audit — 2026-08-18: NO V2 RUN EXISTS YET

Checked per protocol. The 08:48 UTC bot commit (12bf7c0) was a V1 run:
touched only sealed-prices/heat files; the live workflow on origin/main still
has the v1 step list (fetch → heat → commit). Not the silent-skip trap —
crosscheck steps simply are not wired yet; Tyler has not pasted v2.

Notable from that v1 run anyway:
- 151 ETB artifact healed IN PRODUCTION: median $600, n=132, live (tuned
  bounds from 935c282 took effect). The -70.6% spread signal in
  data/divergence-report.json is now stale-by-provenance (generated 08:39
  against the pre-heal median) and will disappear at the first v2 run or
  next manual divergence pass. Do not cite it.
- crosscheck-history remains day-1 only (56 rows, 2026-08-18) — correct,
  since only the manual run has ever fetched crosscheck data.

When v2 IS pasted, the first-run audit must verify: (1) sealed-crosscheck
updatedAt advances past 08:39:09Z by CI, (2) day-2 history rows, (3) both
engine reports regenerated by CI, (4) 151 ETB spread ≈ -4% no signal,
(5) the silent-skip trap: green run + stale crosscheck = missing/wrong
POKEMONPRICETRACKER_API_KEY secret — fix is web step A in the staged file
header.

---

## UPDATE (same day, 09:04 UTC): V2 FIRST CI RUN VERIFIED ✅

Commit 6fcf0ff is the first true v2 CI run. All checks pass:
1. sealed-crosscheck.json updatedAt = 09:04:42Z — advanced by CI ✅
2. divergence + supply-watch regenerated by CI (09:04:42Z stamps) ✅
3. **151 ETB artifact healed: +3.9%, signal=false** (was -70.6% stale-input) ✅
4. Secret works — no silent skip ✅
5. Day-2 history row: definitionally tomorrow (same-day rows are replaced,
   idempotent) — verify at the Aug 19 04:00 UTC scheduled run.
Cosmetic: the pasted workflow retains the staged file's instruction header
as YAML comments; harmless, Tyler may trim via web edit.
