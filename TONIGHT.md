# 🌙 TONIGHT — the one-document fire-list (Aug 18)
*Open this at the PC. Top to bottom, ~15 min total. Everything else already runs itself.*

## 1. Boot → CC terminal → `/rc` → scan QR
Pocket-CC forever. Then `/config` → "Enable Remote Control for all sessions" → true.

## 2. Feed CC one prompt (covers key + all its work):
> Pull first. Read-Host the PPT key when needed. Then: (a) re-run the singles
> enrichment (limit-20 fix is in) and print the FIRST Grading Premium table;
> (b) wire isForcedChase + bestPrice into the candidate generator's ranking,
> regenerate research/watchlist-candidates.md, then run
> verify-watchlist-prices.mjs across every proposed row (batch the ids) and
> commit sheet + verify file — curation is blocked on this; (c) relay your
> trapped campaign report + v2.2 print status. Fences standard.

## 3. Discord webhook (optional but 🔥): Server Settings → Integrations →
Webhooks → New → pick a channel → Copy URL → GitHub repo Settings → Secrets →
Actions → New secret: `DISCORD_WEBHOOK_URL`

## 4. Paste v2.2: repo → research/workflow-v2.2-products-alerts-READY.yml →
Raw → Ctrl+A/C → .github/workflows/update-sealed-prices.yml → ✏️ → Ctrl+A,
Del, Ctrl+V → Commit. (Header in the file repeats this.)

## 5. Actions → Run workflow → the first FULL-machine run: ~150 sealed,
101 crosschecked, Pulse+Board publish themselves, Discord pings if step 3.

## 6. Two sends from the couch:
- PPT licensing email — body in research/ppt-licensing-note.md (find their
  support email on pokemonpricetracker.com, or their Discord)
- eBay Insights application — text in research/insights-application-draft.md,
  file at developer.ebay.com (10 min, expect denial, free lottery)

## 7. Relay here: CC's report + the Premium table + regenerated sheet status.
Then tomorrow's phone-curation runs on VERIFIED lines.

## 10-second answers whenever: eBay seller account y/n? · Day-2 line A or B?
· SwarmFall one-liner (announcement's staged and waiting)
